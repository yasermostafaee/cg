node_modules
dist
build
out
release
coverage
.turbo
.claude
pnpm-lock.yaml
*.tsbuildinfo
# Generated starter fixtures — regenerated by @cg/template-fixtures build
fixtures/templates/

# Project blueprint — preserved verbatim; do not format
README_PROJECT.md

# Bundled template-runtime — generated by scripts/bundle-runtime.mjs
apps/designer/resources/template-runtime

# Generated CasparCG runtime bundles (ESM + IIFE) — by @cg/single-file-export's
# scripts/bundle-runtime.mjs (regenerated each build; never committed).
packages/single-file-export/src/generated/

# Gitignored generated files some dev flows still write into the Designer app
# (apps/designer/.gitignore covers them) — keep `pnpm format:check` genuinely
# green when they exist.
apps/designer/src/generated/

# Generated harness output — written to cwd by @cg/caspar-amcp-probe (C-001 Phase 3b)
caspar-amcp-probe-*.results.json
caspar-amcp-probe-*.wire.ndjson

# Committed capture evidence — raw tool output, kept byte-identical to what the
# tool emitted. Formatting it would edit the record: prettier appends a trailing
# newline and collapses short arrays onto one line, so the committed file stops
# being the artifact the run produced.
tools/*/evidence/**

# Captured evidence, same rule as `tools/*/evidence/**` above: the plant's CIAB
# client (a MODIFIED CasparCG Client) tool definitions, kept byte-identical to the
# artifact the owner captured. Formatting it would edit the record — and this one
# is cited by entry name from `live-source-multibox` design.md and from C-021/C-023,
# so its line numbers are load-bearing.
docs/recon/ciab-client-tools.json
