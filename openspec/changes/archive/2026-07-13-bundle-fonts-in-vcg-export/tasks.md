# Tasks — bundle fonts in the `.vcg` export

## 1. Red-first tests

- [x] 1.1 Exporter test: a scene with a bundled (`asset-*`) font exports a `.vcg`
      whose `fonts/` holds the font bytes AND whose manifest `assetIndex` has the
      matching `kind: 'font'` entry
- [x] 1.2 Exporter test: the package's CSS/HTML declares an `@font-face` for the
      bundled family referencing it package-relative — no external / `file://` ref
- [x] 1.3 Measurement test: a content-driven ticker resolves its face from the
      PACKAGE (not a system install) — the packaged bytes reach the re-render's
      font source, so the crawl measures font-correct width
- [x] 1.4 Warning test: `vcg-ticker-fonts-not-bundled` no longer fires when the
      ticker's font is bundleable, but STILL fires (naming the family) for a
      system font
- [x] 1.5 Round-trip test: the font-carrying `.vcg` packs → verifies → unpacks with
      the font bytes intact
- [x] 1.6 Show the RED run (all of the above fail before the fix)

## 2. Implement

- [x] 2.1 `Exporter.ts` — `#gatherFonts`: resolve each `scene.fonts` `asset-*`
      family's bytes from the AssetStore; key them into a `fonts` map as
      `fonts/<sha256><ext>`; push a `kind: 'font'` `assetIndex` entry per font
- [x] 2.2 `Exporter.ts` — pass the map to `pack({ …, fonts })`
- [x] 2.3 `Exporter.ts` — `buildIndexHtml` bakes an `@font-face` per bundled font,
      package-relative `url('./fonts/…')`, so a standalone package is self-contained
- [x] 2.4 `Exporter.ts` — re-scope the `vcg-ticker-fonts-not-bundled` preflight:
      fire per-font only for a ticker font that cannot be bundled; new message
- [x] 2.5 Skip-not-fail policy for system/licensed/missing-bytes fonts (design.md)

### Ripples chased

- [x] 2.6 `exporter-vcg-preflight.test.ts` — the original D-028 assertion ("ANY
      ticker warns") is SUPERSEDED and was rewritten: its ticker uses `Vazirmatn`,
      a face the Runtime app ships and inlines itself, so warning on it would be a
      lie
- [x] 2.7 `apps/runtime/tests/template-delivery.test.ts` — added the on-air contract
      test: a font-carrying `.vcg` gets its packaged face base64-inlined into the
      HTML the bridge serves (locks the "no Runtime/bridge change needed" claim)

## 2b. Owner verification found NO on-air change — investigated, root-caused

- [x] 2b.1 Reproduced the served path end-to-end in Chromium (unpack -> re-render ->
      the HTML the bridge serves). The served HTML DOES inline the packaged face as a
      `data:` URI; the CSP hypothesis is refuted
- [x] 2b.2 Root cause of "no difference": `scene-builder` appends `Vazirmatn` behind every
      family and the Runtime app inlines its own Vazirmatn, so a missing bundled Vazirmatn
      falls back to Vazirmatn. A Persian fixture CANNOT discriminate (see design.md)
- [x] 2b.3 A/B'd a force-loading font gate against the existing `document.fonts.ready`
      gate on the real served HTML: 3.34s vs 3.36s delta — IDENTICAL. The measurement-race
      hypothesis is refuted; no runtime change made
- [x] 2b.4 Ground-truth fixture that CAN discriminate: Exo 2 Latin @400 (see design.md)

## 3. Green gate

- [x] 3.1 `pnpm --filter @cg/designer typecheck lint test build`
- [x] 3.2 `pnpm --filter @cg/runtime typecheck lint test build` (the other touched
      workspace; `@cg/vcg-format` needed no source change — its `fonts` seam already
      existed)
- [x] 3.3 `pnpm turbo run typecheck lint test build --force` → 79/79, 0 cached;
      `pnpm format:check` clean
- [x] 3.4 `pnpm openspec validate bundle-fonts-in-vcg-export --strict`

## 4. Owner verification on REAL CasparCG (definition of done)

- [x] 4.1 Produce an exportable `.vcg`, generated by the REAL Exporter
- [x] 4.2 State the false-pass caveat: a system-installed copy of the font renders
      correctly even WITHOUT bundling — hence the paired control package and the
      `asset-*` family name, which no OS font can claim
- [x] 4.3 **The Persian pair cannot prove anything** (the Vazirmatn fallback masks a
      missing bundled Vazirmatn — see design.md). Ship the GROUND-TRUTH pair instead:
      `crawl-latin-bundled.vcg` / `crawl-latin-control.vcg`, an Exo 2 Latin face at
      weight 400. Measured in Chromium against the real served HTML: **63.10s vs
      59.76s** on-air duration, and visibly different letterforms
- [x] 4.4 **OWNER-VERIFIED on real CasparCG.** The Latin pair shows BOTH a clear
      letterform difference (heavy Exo 2 vs light Vazirmatn) AND a crawl-duration
      difference (~63s bundled vs ~60s control). That family is installed nowhere, so
      the difference can only come from the package: the packaged font drives the
      content-driven measurement. D-121 works end-to-end.
