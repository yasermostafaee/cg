# Session B — finish the batch, unattended

**Read at `1577c4b968cf0dddda099fea4e9e6b5b03a7e9c7`** (`git pull --ff-only` → "Already up to date";
the remote WAS reachable, `origin/dev` at `089a12ebd41f8113f2f050e79d2f331e1f704d0b`).

⚠ `tools/caspar-bridge/src/template-http-server.ts` — the owner's uncommitted `192.168.21.93` hack —
was never staged, committed or reverted.

**Written incrementally, item by item, per the unattended-mode instruction.**

---

## §0 — the unpushed queue, accounted for

**9 commits were already local at session start, and all 9 are session A's.** The brief inferred
they predated session A; they do not.

Session A began at `089a12eb` (== `origin/dev`, clean apart from the hack), committed the carried-over
PRD corrections as `be2e390c` as its FIRST act, and only then pulled and reported HEAD. Timestamps
settle it: `089a12eb` 18:43:19 (the pushed tip), `be2e390c` 19:00:52, the rest 19:05 onward.

**Nothing is unaccounted for, so the push is not held on §0's ground.**

---

## Item 1 — B-139 task 4.6 (`fa5d4607`)

One DOM test, narrowed as instructed: `apps/runtime/tests/layersPanel.plateDirty.dom.test.ts`.

The "both surfaces agree" matrix is still NOT built and that argument stands. What it does not cover
is the VERB — a predicate returning `true` does not prove a button is enabled, and the half of B-139
that hurt was that UPDATE was DISABLED for a staged un-assignment.

**Verified RED on both halves** against the pre-fix collapsed baseline:
`the row should show a draft chip: expected null not to be null` AND
`UPDATE must be ENABLED …: expected true to be false`. Green after.

Both facts are `expect.soft` deliberately — a hard failure on the chip would short-circuit before the
verb was examined, so the red run would only ever have proven half of what the test is for.

Instrument notes: UPDATE is a `surface: 'menu'` action so it is absent from the DOM until the row's
context menu opens, and the menu is portalled to `document.body`; a menu item is a `role="menuitem"`
div, so "disabled" is `aria-disabled` — reading `.disabled` would be `undefined` and assert nothing.

## Item 2 — R-055 follow-up (`2478a9ac`)

Wire names left alone, per the brief. Pointer comments added at `PlayoutLayerState` and at
`PlayoutLayersStateChannel` saying the operator-facing name is **STATION LAYERS**, that the wire names
are deliberately unchanged, and where the reasoning lives. The rename itself is recorded as a
follow-on **inside R-054**, which reopens those files anyway.

## Item 3 — P-033 and P-034 filed (`a71a9604`, `60e6255a`)

Sweep confirmed highest `P-` was `P-032`; neither number occurred anywhere in `docs/`.

- **P-033** — `typecheck` covers `src/**` only. Measured: **557 test files** outside the compile
  guarantee (113 runtime, 203 designer, 241 packages+tools). ⚠ **The Designer does NOT differ.** It
  narrows the very guarantee B-139's fix rests on. Three owner options with costs; no `tsconfig`
  touched.
- **P-034** — the one unreproducible Designer E2E flake. **The failing test's NAME was not captured
  and is NOT guessed**; the two specs using that assertion form are named as candidates, with the
  time-dependent one flagged as plausible on its shape alone.

## Item 4 — B-140 (`af8a0762`, corrected by `a3635484`) — **COMPLETE**

A new **`@cg/gesture`** workspace package holding ONE headless drag hook, consumed by the Runtime's
`ShellDivider` and the Designer's `Splitter`. Both apps migrated; neither owns gesture code now. A
row was added to `CLAUDE.md`'s "Where features go" — **shared config the next session must pull.**

23 unit tests (100% statements, 90% branches) drive the terminator matrix case by case. The shield
REPLACES the `document.body` cursor / `user-select` writes rather than joining them, so the
application-wide stuck state has no home. Runtime hit area 24px, visual still 6px.

### 🔴 Two corrections, and the second is the more useful

**1. The iframe cause is DISPROVEN.** With a positive control asserting the release point hit-tests
to an `IFRAME`, and a rebuild between runs, **the pre-fix divider ended that drag correctly.** Lead:
the rehearsal frame carries no `sandbox` attribute, so it is same-origin and Chromium's implicit
mouse capture already keeps `mouseup` with the originating document.

**What reproduces is an ending that is not a `mouseup`** — window blur. The old divider listened for
`mouseup` and nothing else. That is the reported symptom ("the drag releases but the line stays
blue") through the door that is actually open, and the E2E now pins it **RED pre-fix, GREEN after**.

The PRD item's title and Why, and the change's proposal, were rewritten so the disproven story does
not outlive its truth. The Designer half is re-anchored the same way: its permanently-stranded
listeners are real, but reached via blur / `pointercancel`, not via its canvas iframe.

**2. ⚠ I reported a defect in my own fix that did not exist, and the cause is worth more than the
retraction.** I recorded that the fix left the divider painted as dragging after a blur. It was a
**stale build**: `test:e2e` serves the built `dist/`, and `pnpm --filter @cg/runtime test:e2e` does
**not** rebuild — so every red/green comparison after the first build was executing the SAME bundle.
`CLAUDE.md` warns about precisely this. **A red/green comparison is evidence only if the artifact was
REBUILT between the runs; two runs of a stale bundle agree perfectly and prove nothing.**

The failing case was restored (not left deleted) and is task 5.6.

## Item 5 — B-141 (`<see log>`) — **PARTIAL, deliberately**

`--audit-log-path` → `~/.cg-runtime/bridge-audit.ndjson`, following the
`--source-assignments-path` precedent exactly and never under `templatesDir` (B-116). **`@cg/audit`
goes from dead code to the source of truth**: the writer is constructed in `CasparRuntime`,
`#recordAudit` is the one append helper, `auditRecent` reads its tail, and `auditHealth()` exposes
`{ configured, path, errorCount, lastError }`.

**A failed audit write can never take the station off air** — fire-and-forget, the writer reports and
keeps trying. The contrast with the config stores is stated at the declaration: those are
preconditions for correct playout; an audit entry is a record of what already happened.

🔴 **ONE definition of the action set.** The panel's filter literal had drifted to **eleven** against
the schema's **fifteen**, so `stop`, `next`, `update-deferred` and `update-installed` could never be
isolated even once written. It is now derived from `AuditEntrySchema.shape.action`.

Wired: `reconnect`, `lock-engage`, `lock-release` (both refusal paths), `failover`. The PIN is never
recorded.

**NOT wired, and why** — `load`, `take`, `update`, `out`, `stop`, `remove`, `next`. Each is a
multi-branch method with several refusal returns; a single append at the end would record `ok` for
paths that refused, and **a forensic record that misreports an on-air action is worse than no
entry.** `export` and `update-deferred` / `update-installed` have **no operation in this process**
and are named as unwireable rather than invented. The panel's three empty states need `auditHealth`
over IPC and are not built.

---

## Gate and E2E

- **`pnpm gate` — GREEN.** 89/89 tasks, `0 cached, 89 total` (89 not 85: the new `@cg/gesture`
  workspace), prettier clean, `openspec validate --all --strict` 56/56.
- **`pnpm gate:e2e` — GREEN.** Runtime **81/81** (78 + the three new divider cases), Designer
  **268/268**. Run through the bounded entry point, which rebuilds first.
- ⚠ A Windows pass **discharges nothing**. Every affected `tasks.md` says
  **"Linux `e2e` still owed — no run URL."**

## The push

Attempted once at the end of the session, after the gate and the E2E. **Whether it landed is
verifiable directly: `git log origin/dev..HEAD --oneline` — empty means it landed.** §0 was clean, so
nothing held it on that ground.

## 🔴 What the next session must know

1. **The workspace gained a package — `@cg/gesture` — and `CLAUDE.md` gained a row for it.** Shared
   config: pull before doing anything, and run `pnpm install`.
2. **B-140's original cause is disproven.** Do not repeat the iframe story; the reproducing
   terminator is window blur. Both the PRD item and the change say so.
3. **A red/green comparison is evidence only if the artifact was REBUILT between the runs.**
   `pnpm --filter @cg/runtime test:e2e` does NOT rebuild, and two runs of a stale bundle agree
   perfectly while proving nothing. This produced a false "defect in the fix" report inside this very
   session, which is retracted in `shared-drag-gesture/tasks.md` §6b.
4. **B-141 is PARTIAL** — seven append sites and the panel's three empty states remain, listed in
   `audit-writer-forensic-lite/tasks.md` §5 with the reason each was left.

## What was NOT started

The follow-on queue (`live-source-multibox` tasks 1.5c UNIT C, 1.5f, 1.5e, 1.5d, 8.3) was **not
begun.** The session's own brief items ran to their end first, and starting a pixel-level assertion
on an exported artifact — the largest of them — was not something to begin at this point rather than
do properly. Nothing in that change was touched, so its owner gates are all intact.
