# Handoff — 2026-08-11, session H (unattended evening run)

**Nothing was implemented and nothing was fixed.** This session filed requirements and diagnosed two
bugs from code and git history only. `git diff --stat` for the whole session touches
`docs/**` and nothing else — no source file, no test, no build config, no worktree, no stash.

Two pushes, both verified against `git ls-remote origin dev`.

## What was filed

| Item      | File                   | Status                                                                            |
| --------- | ---------------------- | --------------------------------------------------------------------------------- |
| **D-133** | `docs/prd/designer.md` | EXTENDED + retitled, medium → high — unconditional loop authoring, seam semantics |
| **D-135** | `docs/prd/designer.md` | EXTENDED + retitled, medium → high — PLAY drives the canvas, not only scrubbing   |
| **D-151** | `docs/prd/designer.md` | NEW — add-time duration guard (client-required)                                   |
| **P-032** | `docs/prd/platform.md` | NEW — the last surviving legacy compatibility shim (from the addendum)            |
| **B-136** | `bugs-designer.md`     | NEW — video never visible in PVW                                                  |
| **B-137** | `bugs-designer.md`     | NEW — video stays paused in the preview after a scene rebuild                     |

`D-133` and `D-135` were **extended rather than duplicated**: the PLAY requirement is the same
machinery and the same carve-out as the scrub requirement already in D-135, so a sibling item would
have been two headings with one implementation. Retitling is not renumbering — nothing citing either
number breaks. The registry records both mints and the sweep behind them.

**Duplicate check result, in one line:** nothing anywhere in `docs/prd/**`, `DEBT.md` or
`DEBT-SWEEP.md` covers the add-time duration guard (confirming your own check), and nothing covers
either video bug.

## Regression verdicts — both NOT A REGRESSION, established from git

⚠ **First, a correction to the brief's premise:** `94be0efb` is **not** the head prior to session D
— it **is** the B-129 fix itself. The true prior head is `bd9dc57`. Everything below used
`94be0efb..HEAD`, which is the widest honest window since it contains B-129.

- **B-136 — NOT A REGRESSION, and never worked.** `git log -S"media-src" -- apps/runtime/index.html`
  returns **zero commits**: the Runtime page has never carried that CSP directive, across all 8
  commits that file has ever had. The Designer gained it in `aa0138a` (D-128 Phase 2), exactly when
  it first needed to play video; the Runtime was never given the equivalent. An original gap, not
  something recent work broke. D-150/B-104 and B-129/B-133/B-134 were both diffed and are clear —
  the whole asset-resolution and video-render path is absent from the changed-file list.
- **B-137 — NOT A REGRESSION.** Every file that could produce it is untouched in that window;
  `ticker-driver.ts` last changed 2026-06-26, and the structure that produces the defect dates to
  2026-07-23/26. The `.cgproj` build is where you _noticed_ it, not where it entered.

**Mechanisms — code-derived, not observed.** B-136: the Runtime page's CSP has no `media-src`, and a
`srcdoc` iframe inherits its embedder's policy, so the `data:video/webm` bytes are refused before
decoding. ~1 line to fix. B-137: the preview transplants the old `<video>` node back over the freshly
built one, but nothing re-points the new `VideoDriver`, so the driver commands a detached src-less
orphan while the visible node is the one teardown paused. ~20 lines to fix. **Two independent
investigations converged on the B-137 mechanism from different starting points**, which is why it is
stated with confidence despite no live run. Neither bug's fix collides with `live-source-multibox`
phases 5–6, and a real live source would **not** join the driver set — these do not get worse at
phase 6.

## Open questions recorded (all live in the items, not here)

1. **D-151** — does the add-time dialog need a third "add anyway, it will loop" choice, or is
   decline-means-not-added firm? A looping background video in a short scene is a legitimate pattern
   and loop intent is unknowable at add time. Both candidates stated; **deliberately unresolved.**
2. **B-137** — the evidence does not separate "any ANIMATING element breaks it" from "only a
   timeline DRIVER breaks it", because every known-good companion is static and every known-bad one
   is time-driven. Filed unresolved. Code review proposes a third answer (it is simply _what forces a
   rebuild_) that would dissolve both; experiment 3 decides it.
3. **B-136** — which surface "PVW" names. The literal `PREVIEW (PVW)` panel is the Runtime's; the
   Designer's modal is titled `Preview · WxH`. Filed against the Runtime; if you meant the Designer
   modal the CSP mechanism does not apply.
4. **B-137, experiment 5** — whether the `infinite` / `cycle seam` checkbox you toggled was the
   preview modal's own control or the Inspector's. If it was the Inspector's, a second undiagnosed
   mechanism is in play, because the modal renders a snapshot an Inspector edit cannot reach.

## What to do first when you resume

1. **Run B-137's experiment 2** (one minute) — video alone, no ticker, no Lottie; change any preview
   timing knob. It confirms or kills the mechanism, and it is the cheapest decisive test in either
   item.
2. **Run B-136's DevTools check** (one minute) — put a video template into REHEARSE and look for
   `Refused to load media from 'data:video/webm…'`. Present ⇒ proved, and the fix is one line.
3. **Answer B-137's experiment 5** from memory if you can — which checkbox you actually toggled.
4. **Check the B-136 availability rider**: does the Project Assets panel still list the video after
   reopening the `.cgproj`? This is the one way D-150 could be causal without appearing in a diff,
   and it exposes a real latent trap either way — `preview.ts`'s unresolved-asset branch has an
   `IMG` leg only, so a missing VIDEO is silently invisible with no marker and no console line.
   **Worth fixing regardless of how B-136 resolves.**
5. **Decide P-032** — remove the `PlayoutSchema` legacy shim or keep it. The compatibility floor is
   not set until first client delivery, so the window for a cheap removal is open and will close.
6. **Decide D-151's open question** before anyone implements it.

## What I expected to find and did not

- **A "client-required" tag in the PRD house style.** There is none — the convention is prose in the
  **Why** field. I marked all three in the priority field and in the text.
- **A place for session notes.** There is no `docs/handoff/` convention; this file creates one.
- **A shared PVW surface.** I expected the Designer's preview and CG Control's PVW to be one
  component. They are two separate implementations sharing only `@cg/template-runtime`'s scene
  builder — which is _why_ B-136 is Runtime-only and fixable in a line.
- **Support for either hypothesis the brief proposed for B-137.** Autoplay policy is refuted (the
  element is muted, and muted autoplay is permitted); the stepped/hold frame driver **does not
  exist** in this engine. Both are recorded as refuted in the item so nobody re-runs them. The
  related worry that the hold-driver set is computed in several disagreeing places was also checked:
  six sites, and they agree today.
- **Any live observation.** By design — nobody was at the machine. Every claim here is from code or
  git; the experiment lists are what convert them into evidence.
