# Handoff — 2026-08-13, session X (the owner's file: one hypothesis refuted, one measured as designed)

Commit(s) on `dev` per §5. Nothing merged to `main`; no item number minted; nothing archived.
Change dir: `openspec/changes/media-phases-follow-composition/` (design.md §10.1 answered,
§10.2 added).

---

## 1. The file, and what each surface actually receives

`videotickeroutrobug.cgproj` matches the brief's numbers exactly (root: no lifecycle, no layers;
comp1: `outPoint 525`, `contentStart 251`, `auto-out` + `content-driven`; the 14.32 s follow
video; the repeat-2 drain ticker with `lifespan {251 → 525}`). The brief's mandated first
establishment, logged on the real file, not guessed: **the Designer hands `applyScene` the active
composition PROMOTED to the scene root** — `editSceneOf` for the canvas, `scopeSceneToComposition`
for the preview — lifecycle, frameRange and layers included. The runtime never receives the raw
document.

## 2. Hypothesis 1 (canvas anchors) — REFUTED on the real file

Because of the promotion, `scrubOutPoint = scene.lifecycle?.outPoint` IS comp1's out point for
everything the canvas shows. Measured through the real store load + real projection + runtime,
on the owner's own numbers: `t(524) = t(525) = 5.020` (H), `t(526) = 5.040`, `t(600) = 6.520` —
**the canvas boundary is exact at `2f2d6221`.** The predicted `outroElapsedMs`-always-null state
cannot occur from any Designer surface. No canvas fix is owed, and a failing-first fixture for
this shape cannot be honestly written at this tree — said loudly, with the measurement, instead
of a fix that would have nothing to fix. The projection contract the canvas depends on is now
PINNED (`apps/designer/tests/follow-document-shape.test.ts`: promotion fields + the one-frame
boundary on the owner's numbers, video and lottie).

## 3. Hypothesis 2 (preview delay) — CONFIRMED as (a): the content-driven hold, by design

Measured on the real projection: the intro reaches the out point at **10 500 ms** wall; the
ticker's two-pass drain crawl (running since content start, 5 020 ms) completes at **≈ 15 440
ms**; the video leaves `H` at **15 440 ms — 4 940 ms after the out point, exactly the crawl's
remainder** — then plays its follow outro to the derived end (8 840 ms) correctly. Under
`holdSource: 'content-driven'`, `outPoint` is where the HOLD begins and the hold lasts until the
content completes: **the outro cannot begin at the out point by construction.** This is the
shipped design working as specified — no fix applied, per the brief. (Harness text metrics set
the absolute number; real glyph widths change it, not the mechanism.)

Pinned in `follow-outro.test.ts` with a TIMED-hold control: same scene, `holdMs: 200` ⇒ the
outro starts at `outPoint + 200 ms` — the delay is the hold source and nothing else.

🔴 **Your decision, recorded as OPEN in design.md §10.2** — for this scene, pick one:
keep content-driven (the crawl is the clock); switch to a TIMED hold (outro at
`outPoint + holdMs`); shorten the content (fewer repeats / faster crawl); or price a DESIGN
change ("the OUT interrupts unfinished content") separately — it has on-air consequences.

## 4. The lifespan answer — input for session W, measured and pinned

Your workaround attempt (`lifespan.out = 525`, the out point) cannot shorten the wait: during
the hold the held frame IS the out point, the gate keeps the ticker visible and crawling, and
the trimmed scene leaves `H` within one tick of the untrimmed one (pinned). **The lifespan gate
is VISIBILITY-only — it neither ends a content driver nor detaches it from the hold.** Recorded
in design.md §10.2 for session W's design.

## 5. Verification

- Suites: designer 1177, template-runtime 860 — green; `pnpm gate` green uncached on the final
  tree; `openspec validate media-phases-follow-composition --strict` green. All session-V
  boundary pins stay green (the root-level case did not regress).
- §10.1 folded as ANSWERED: a follower's outro room stays `[outPoint → activeRange.out]`;
  the ruler-room alternative REJECTED; both candidates kept as reasoning.
- Linux `e2e` DISCHARGED for `8e3b8f13` —
  <https://github.com/yasermostafaee/cg/actions/runs/31718992350>, run `conclusion: success`,
  `E2E (Playwright)` job **RAN** (`completed success`, not skipped). This SUPERSEDES session
  V's run for `c518f999` (<https://github.com/yasermostafaee/cg/actions/runs/31707420095>) —
  the debt follows the pushed HEAD.

## 6. Visual check (your file)

Scrub to frame 525 — held look; 526 — already moving (measured; if your canvas still shows
otherwise, reload the Designer tab first — a canvas iframe from a session predating `710f0ab0`
carries the old runtime bundle without video positioning). PLAY the full cycle: the wait you see
before the outro is the ticker finishing its two passes — 4.9 s of crawl remaining at the out
point in the harness's metrics; switch the composition to a timed hold to see the outro start at
the out point + holdMs.

## Not in this session

D-133's implementation, session W's design (its lifespan input is recorded), minting any number,
archiving, merging to `main`.
