# Handoff — 2026-08-13, session V (the follow outro: one silent zero, one measured non-delay)

Two commits on `dev`: `aae192c7` (the fix family) and the boundary-pin/docs commit that closes
the session (see §5 for the discharge). Nothing merged to `main`; no item number minted; nothing
archived. The change dir is `openspec/changes/media-phases-follow-composition/` (design.md
§10–§10.1 + the lottie spec delta's clamps requirement/scenarios).

---

## 1. Two reports, one family — and one measured absence

**Report 1 (freeze forever)** reproduced and is FIXED: the unique configuration producing
"intro perfect, H perfect, outro never" is `outSpanMs = (activeRange.out − outPoint) × frameMs
= 0` — the out point at the end of the active range, with the visible "room after it" living
only in the frameRange (the ruler's tail, which neither the OUT segment nor air ever plays).
The collapsed field in the constructed `VideoDriver` options: `outroEndMs` (== `outroStartMs`
== `H`). Manual phases never consult the anchors, which is why the control worked.

**Report 2 (outro starts LATE, by ≈ the comp's `[active.in → outPoint]` span)** was MEASURED
per the brief's step 0 — **and the fingerprint does not reproduce, at HEAD or at `a553f79d`**
(the runtime positioning paths are identical; `aae192c7` touched schema/designer only):

- Canvas, seven variants (holdAt ±, contentStart ±, `activeIn > 0`, authored idle, out point at
  60/100/140 as a tracking sweep): `t(outPoint) == H` exactly, first motion at `outPoint + 1`
  frame, every variant; moving the out point moves first motion WITH it.
- Preview, both exit mechanisms: `out()` → first motion within one 20 ms step; auto-out (timed
  hold; comp intro 2.8 s + hold 0.4 s) → outro at 3240 ms vs 3200 expected — timer granularity,
  not a comp-anchored offset. No subtraction in the outro path can produce a comp-intro-sized
  delay: drivers receive only clip-side ms.

Said loudly, per the brief's own fallback: **the owner's ~140 was not a live wrong-anchor bug at
this tree.** Both observations are consistent with the zero-OUT-segment family: the freeze IS it,
and "motion later than the out point after pulling it earlier" fits a SHORT clamped outro window
on a PINNED active range — brief motion, then the held clamp, read as lateness. If it recurs,
**the scene file is the decisive artifact** — the new pins prove the pinned shapes cannot be the
failing one.

## 2. What was fixed and hardened (`aae192c7` + this session's pins)

- **`noOutSegment` clamp flag** in `followWindowMs` + the Inspector hint (both kinds, one
  shared surface): a zero-OUT-segment follower now explains itself and names the way out — drag
  the out point earlier, or extend the active range past it (§9.1's rule: inert behaviour
  explains itself).
- **`clampLifecycleTo` — THE one lifecycle-invariant rule** (`state/slices/document.ts`),
  applied by `setLifecycle` AND both window-movers. Before it, `setSceneDurationFrames` (shrink)
  and `setSceneActiveOut` could STRAND `outPoint` outside the active range — a scene
  `refineLifecycle` REJECTS, i.e. **a save that could not re-load** — and shrink-then-regrow
  silently PINS `activeRange.out` below the ruler (that pin is how "out at 72 of 150" arises;
  the out-marker drag then clamps silently onto the collapse point).
- **Runtime-level exit coverage for follow media** (`follow-outro.test.ts`, 13 tests): recon
  found NO test drove a follow element through `out()`/`stop()`/auto-out — derivation, registry
  and ledger were each unit-tested, never their composition. Both kinds now run play → hold-at-H
  → exit; the zero-OUT-segment shape is pinned loudly as rule-derived; and the sharpened brief's
  discipline is kept as **exact one-frame boundary pins** (canvas `t(outPoint+1)` has already
  left H; the boundary TRACKS the out point; auto-out leaves H within timer granularity of the
  OUT start) — a wrong-anchor regression now fails one frame past the out point, not
  "eventually".

## 3. Deliberately NOT changed, raised for your call

The outro rule stands: the OUT segment is `[outPoint → activeRange.out]` in all four places
(air, preview lifecycle, canvas mapping, follow derivation) — consistent. If a follower's outro
should instead track the ruler's room after the out point (`frameRange.out`), that is a rule
change; the evidence for pricing it is that the ruler shows frames the composition will never
play, and until this session nothing said so.

## 4. Failing-first evidence

6 schema flag tests, 3 Inspector hint tests, 4 store invariant tests — red before `aae192c7`'s
fix (key-absent / hint-absent / stranded-marker-parse-failure). The exit suite and the boundary
pins were green from first run — that green is itself a finding (the runtime paths were never
the fault) and is kept as the missing coverage. No shipped test changed in this session.

## 5. Verification

- Suites green: shared-schema 397, template-runtime 857 (incl. the 13 follow-outro tests),
  designer 1174; `pnpm gate` green uncached on the final tree; `openspec validate
media-phases-follow-composition --strict` green.
- Linux `e2e` DISCHARGED for `c518f999` (this session's final HEAD) —
  <https://github.com/yasermostafaee/cg/actions/runs/31707420095>, run `conclusion: success`,
  `E2E (Playwright)` job **RAN** (`completed success`, not skipped). This SUPERSEDES session U's
  run for `710f0ab0` (<https://github.com/yasermostafaee/cg/actions/runs/31683317925>) — the
  debt follows the pushed HEAD. `aae192c7`'s own green run
  (<https://github.com/yasermostafaee/cg/actions/runs/31705293896>, `E2E (Playwright)` RAN)
  verified the fix commit's tree and joins the history.

## 6. Visual check (your scene)

If the follow panel shows the amber "no OUT segment" hint: that is the diagnosis — drag the out
point left or extend the active range past it, and the outro plays from the out point exactly
(park the playhead ONE frame past it: the video has already left its held look). Manual marks:
unchanged. If you can reproduce the ~140-frame delay after this, save the project file as-is —
it is the artifact the measurement needs.

## Not in this session

D-133 (`tasks.md` §1–§3 — next), B-138, the `updateElement` grouped-element defect, the
paste/cycle-guard hole, the raised outro-rule question (yours), minting any number, archiving,
merging to `main`.
