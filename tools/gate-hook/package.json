{
  "name": "@cg/gate-hook",
  "version": "0.0.0",
  "private": true,
  "description": "P-009 - unit tests + typed surface for the pure decision logic behind .claude/hooks/gate-stop.mjs (the Stop hook that keeps a Claude Code turn from ending with a red local gate). The logic itself is src/gate-decision.mjs: plain zero-dependency ESM with NO build step, imported by the hook via relative path so it works on a fresh clone.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json --tsBuildInfoFile typecheck.tsbuildinfo",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@cg/eslint-config": "workspace:*",
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
