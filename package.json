{
  "name": "cg",
  "version": "0.0.0",
  "private": true,
  "description": "Broadcast CG Platform — Designer + Runtime for CasparCG playout",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "node tools/gate-hook/src/bounded-turbo-cli.mjs run test --continue=dependencies-successful",
    "test:integration": "node tools/gate-hook/src/bounded-turbo-cli.mjs run test:integration --continue=dependencies-successful",
    "test:e2e": "turbo run test:e2e --continue=dependencies-successful",
    "gate": "node tools/gate-hook/src/bounded-turbo-cli.mjs run typecheck lint test build --force --continue=dependencies-successful && pnpm format:check && pnpm openspec validate --all --strict",
    "gate:e2e": "turbo run test:e2e --force --continue=dependencies-successful --concurrency=1",
    "package": "turbo run package",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "openspec": "openspec",
    "clean": "turbo run clean && rimraf node_modules",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md,yml,yaml}": "prettier --write"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "0.12.15",
    "@ffmpeg/util": "0.12.2",
    "@fission-ai/openspec": "^1.3.1",
    "@vitest/coverage-v8": "^2.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.1",
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
